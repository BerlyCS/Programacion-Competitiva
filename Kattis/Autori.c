#include <stdio.h>
#include <string.h>
int main() {
    char* name, *sh;
    scanf("%s", name);
    printf("%s\n",name);
    
    /* strcat(sh, "" + name[0]); */
    for (int i=0; i<strlen(name); i++) {
        if (name[i] == '-') {

        }
    }
}
